rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // A templates mappa tartalmának olvasását engedélyezzük minden bejelentkezett felhasználó számára.
    match /templates/{allPaths=**} {
      allow read: if request.auth != null;
    }
  }
}
